'use strict';

let
	seconds = 1000,
	minutes = seconds * 60,
	hours = minutes * 60,
	days = hours * 24,
	years = days * 365;
// console.log(`milliseconds in a year: ${years}`);


// EXERCISE 1: Create a Date object with today's date.
let date = new Date();
console.log(date);
