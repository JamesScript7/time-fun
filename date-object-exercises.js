'use strict';

let
	milliseconds = 1000,
	seconds = 60,
	minutes = milliseconds * seconds,
	hours = minutes * 60,
	days = hours * 24,
	years = days * 365;

console.log(`milliseconds in a year: ${years}`);
